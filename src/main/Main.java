package main;

import commonTools.CommonTools;

public class Main {
	/* 
	 * TODO Once the load/select/save functionality
	 * has been abstracted, add it here to select the folder
	 * TODO When the folder has been selected and loaded,
	 * then iterate through every file/folder inside it.
	 * There should be a doStuffFile and doStuffFolder.
	 * TODO Pick and reference the best mp3 tagging library
	 * for my purposes and then test it out.
	 */
	public static void main(String[] args) {
	}

}
