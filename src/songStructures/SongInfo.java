package songStructures;

public class SongInfo {
	/*
	 * -Properties - one for each common important tag, such as Genre, Artist, Album, Year, Title, Track
	 * -Property - one for absolute file path
	 */
	
	/*
	 * -Constructor - takes a Song object and sets each tag property accordingly
	 */
	
	/*
	 * -Get methods, but no set methods - one for each important tag
	 */
	
	/*
	 * -getSong function takes no parameters and uses the filepath property to open the file, create a Song object from it, and return it
	 */
}
