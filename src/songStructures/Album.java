package songStructures;

public class Album {
	/*
	 * -Property - ArrayList of type Song
	 * -Properties - ArrayLists of type String to
	 * hold multiple album titles, genres, artists, 
	 * parent directories, etc
	 * -Property - Arraylist to hold album art
	 */
	
	/*
	 * -Constructor - only one constructor, which only
	 * takes a Song object as a parameter, and then passes
	 * it to the addSong function.
	 * -Disable the default constructor if possible
	 */
	
	/*
	 * -the addSong function takes a Song as a parameter
	 * and adds the Song object to the Song ArrayList and
	 * the relevant song properties to the other relevant
	 * ArrayLists (album title, album art, genre, artist, etc)
	 */
	
	/*
	 * -For each tag ArrayList (album title, genre, 
	 * artist, album art, etc) make a getFirst function and
	 * a function to get the count of distinct tags
	 */
	
	/*
	 * -Make a doStuffAlbum function
	 */
}
