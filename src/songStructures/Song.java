package songStructures;

public class Song {
	/*
	 * -Property - AudioFile
	 * -Property - Tag
	 */
	
	/*
	 * -Constructors - start off with only one
	 * constructor, which only takes an AudioFile
	 * parameter.
	 * -Disable the default no-arguments constructor
	 * if possible
	 */
	
	/*
	 * -Port findReplaceTag and trimTag from Main
	 * -Add a function to get the absolutepath of the file itself
	 * -Add a function to get the absolutepath of the parent directory
	 */
}
